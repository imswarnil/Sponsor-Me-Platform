# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
.next/
/out/

# local dev server pid + log (scripts/dev.sh)
.dev/

# supabase (local stack; keep config.toml, ignore runtime + secrets)
supabase/.branches
supabase/.temp
supabase/.env

# local-only Claude Code config & skills (project-owned, NOT shared to GitHub)
.claude/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.vscode

# Docker
postgres_data/
.env*.local
.env*
!.env.example

# OpenNext build output (Cloudflare Workers)
.open-next/
.wrangler/
cloudflare-env.d.ts

# wrangler local dev vars (secrets — never committed)
.dev.vars
.dev.vars.*
