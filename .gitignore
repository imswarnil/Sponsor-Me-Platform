# Dependencies
node_modules/

# Next.js
.next/
next-env.d.ts
*.tsbuildinfo

# OpenNext / Cloudflare build output
.open-next/
.wrangler/
cloudflare-env.d.ts

# Secrets. `.env.example` is the template and IS committed.
.env
.env.*
!.env.example
# What `wrangler dev` reads locally — generated from .env, never committed.
.dev.vars
.dev.vars.*

# scripts/dev.sh — the background server's pid and log
.dev/

# Local-only Claude Code config, project-owned and not shared
.claude/

# macOS
.DS_Store
